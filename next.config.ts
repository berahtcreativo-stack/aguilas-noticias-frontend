/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com', 'cdn.sanity.io'], // agrega otros dominios si usas imágenes externas
  },
};

module.exports = nextConfig;
